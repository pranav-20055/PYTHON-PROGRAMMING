N = int(input("Enter the value of N: "))
count = 0
number = 1
while count < N:
    sum_of_divisors = 0
    for i in range(1, number):
        if number % i == 0:
            sum_of_divisors += i
    if sum_of_divisors == number:
        print(number, end=", ")
        count += 1
    number += 1
